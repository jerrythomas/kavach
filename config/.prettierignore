# generated by prettier
#
# Patterns are resolved relative to this file's directory, so every entry needs
# a `**/` prefix to match at the repo root and inside workspace packages.

**/.svelte-kit/**
**/node_modules/**
**/dist/**
**/build/**
**/coverage/**
**/.turbo/**
**/.wrangler/**
**/.worktrees/**
**/test-results/**

# Machine-generated (convex codegen) — reformatting is churn codegen undoes
**/_generated/**

# Codegen templates: these carry {{placeholder}} syntax and are not valid
# JS/Svelte on their own, so prettier cannot parse them.
**/src/templates/**

static/**
pnpm-lock.yaml
