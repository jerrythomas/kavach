.DS_Store
node_modules
.svelte-kit
/package
*.local
coverage
inspire
*.log
.turbo
.wrangler
.env
.env.*
!.env.example
!.env.test.example
dist
lcov.info
*.code-workspace
.worktrees
.tmp
test-results/
.eslintcache
# Generated llms docs (copied from docs/llms/ by prebuild)
sites/learn/static/llms/
.playwright-mcp
mockups/
# E2E runtime artifacts (scripts/e2e-*.sh)
*.pid
# Local test fixtures. Must stay ignored, not merely untracked: bumpp runs with
# `all: true`, which stages untracked files too — v1.2.0 shipped a stray epub
# into the release commit that way.
spec/fixtures/
